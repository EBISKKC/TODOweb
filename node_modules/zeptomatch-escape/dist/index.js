/* MAIN */
const escape = (() => {
    const re = /(\\.)|([*?!^{}[\]])|(.)/gs;
    const replacer = (_, $1, $2, $3) => $1 || $3 || `\\${$2}`;
    return (value) => {
        return value.replace(re, replacer);
    };
})();
/* EXPORT */
export default escape;
